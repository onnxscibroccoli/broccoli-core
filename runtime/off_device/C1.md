# Phase C1 — Task plane on Durable Object

## Done when
1. Task JSON schema exists
2. Worker can create/list/get/update tasks in workspace VFS
3. Receipt files land under /workspace/receipts/
4. Termux thin client talks to SPIKE_URL only
5. needs_user is the only hard stop for agentic flow

## Agentic rule
Keep going until status becomes needs_user (secrets, destructive
confirm, money, legal). Everything else is autonomous.

## Checkpoints
- task needs_user / done / failed
- deploy success/fail (CI)

Channels: workspace receipt always; gh issue comment; email later (gated).
