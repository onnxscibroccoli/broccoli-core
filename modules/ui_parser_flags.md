# UI parser
Use `FLAG_DONT_SUPPRESS_ACCESSIBILITY_SERVICES` when registering the accessibility
service so background virtual-display sessions do not crash when system a11y is active.
COORD mapping ties screen (x,y) to binary anchors for KXT verification via xxd.
